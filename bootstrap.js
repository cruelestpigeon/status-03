
(function(){
const KEY='status_local_v5';
function defaultState(){ return { me:{id:'me',displayName:'You',username:'you',bio:'',img:'',header:''}, characters:[], posts:[], dms:{}, lore:'', settings:{} }; }
function loadState(){ try{ const r=localStorage.getItem(KEY); return r?JSON.parse(r):defaultState(); }catch(e){ return defaultState(); } }
function saveState(){ localStorage.setItem(KEY, JSON.stringify(STATE)); }
function sanitizeHtml(htmlStr){ if(!htmlStr) return ''; return htmlStr.replace(/<script[\s\S]*?>[\s\S]*?<\/script>/gi,'').replace(/on\w+=\"[^\"]*\"/gi,''); }
let STATE = loadState();
function uid(p='id'){ return p+'-'+Date.now()+'-'+Math.random().toString(36).slice(2,6); }
function esc(s){ return String(s||''); }
function toK(n){ if(n>=1000) return (Math.round(n/100)/10)+'k'; return String(n); }
function renderApp(){ const app=document.getElementById('app'); app.innerHTML = `<div class="nav card"><button id="btnFeed">Feed</button><button id="btnChars">Characters</button><button id="btnDMs">DMs</button><button id="btnLore">Universe</button><button id="btnSettings">Settings</button></div><div class="container"><div class="left" id="leftCol"></div><div class="right" id="rightCol"></div></div><div class="footer">StatusLocal — Personal RP</div>`; document.getElementById('btnFeed').onclick=()=>showFeed(); document.getElementById('btnChars').onclick=()=>showCharacters(); document.getElementById('btnDMs').onclick=()=>showDMs(); document.getElementById('btnLore').onclick=()=>showLore(); document.getElementById('btnSettings').onclick=()=>showSettings(); showFeed(); }
function showFeed(filterAuthorId){ const left=document.getElementById('leftCol'), right=document.getElementById('rightCol'); left.innerHTML=''; right.innerHTML=''; const compose=document.createElement('div'); compose.className='card'; compose.innerHTML = `<div style="display:flex;gap:8px;align-items:start"><textarea id="postText" class="input" placeholder="Write a post..."></textarea></div><div style="display:flex;gap:8px;align-items:center;margin-top:8px"><select id="postAuthor" class="input"></select><button id="postBtn" class="smalllink">Post</button></div>`; left.appendChild(compose); const sel=compose.querySelector('#postAuthor'); sel.innerHTML=`<option value="me">${esc(STATE.me.displayName)} (you)</option>`; STATE.characters.forEach(c=> sel.innerHTML+=`<option value="${c.id}">${esc(c.displayName||c.username)}</option>`); compose.querySelector('#postBtn').onclick=()=>{ const text=document.getElementById('postText').value.trim(); if(!text) return alert('Write something first'); const authorId=document.getElementById('postAuthor').value; const p={id:uid('post'),authorId,text,createdAt:Date.now(),likes:0,replies:0}; if(authorId!=='me'){ const author=STATE.characters.find(x=>x.id===authorId); const followers=Number(author.followers||0); p.likes = Math.floor(followers*0.03 + Math.random()*followers*0.02); } STATE.posts.unshift(p); for(const c of STATE.characters){ if(c.id===authorId) continue; const rp={id:uid('post'),authorId:c.id,text:`${c.displayName} reacts to: ${text}`,createdAt:Date.now(),likes:Math.floor((Number(c.followers||0))*0.01),replies:0}; STATE.posts.unshift(rp); } saveState(); document.getElementById('postText').value=''; renderFeedPosts(filterAuthorId); }; const postsDiv=document.createElement('div'); postsDiv.id='postsDiv'; left.appendChild(postsDiv); renderFeedPosts(filterAuthorId); const rightCard=document.createElement('div'); rightCard.className='card'; rightCard.innerHTML=`<div style="display:flex;justify-content:space-between;align-items:center"><strong>Characters</strong><button id="newChar" class="smalllink">+ New</button></div><div id="charListSmall" style="margin-top:8px"></div>`; right.appendChild(rightCard); rightCard.querySelector('#newChar').onclick=()=>openCharacterEditor(); renderCharacterListSmall(); }
function renderFeedPosts(filterAuthorId){ const container=document.getElementById('postsDiv'); container.innerHTML=''; const posts=STATE.posts.slice(); for(const p of posts){ if(filterAuthorId && p.authorId!==filterAuthorId) continue; container.appendChild(renderPostElement(p)); } }
function renderPostElement(p){ const wrapper=document.createElement('div'); wrapper.className='post'; const author=(p.authorId==='me')? STATE.me : (STATE.characters.find(x=>x.id===p.authorId) || {displayName:p.authorId,username:p.authorId,img:''}); wrapper.innerHTML = `<div class="post-header"><img class="post-avatar" src="${esc(author.img||'data:image/svg+xml;utf8,<svg xmlns=\'http://www.w3.org/2000/svg\' width=\'120\' height=\'120\'><rect width=\'100%\' height=\'100%\' fill=\'%23ffffff\' /></svg>')}" onerror="this.style.display='none'"><div class="post-user"><div class="post-display">${esc(author.displayName)}</div><div class="post-username">@${esc(author.username||author.displayName||author.id)}</div></div></div><div class="post-divider"></div><div class="post-content">${esc(p.text)}</div><div class="post-divider"></div><div class="post-stats"><div class="controls"><span class="icon">♡</span> <span>${toK(Number(p.likes||0))}</span></div><div class="controls"><span class="icon">⇄</span></div><div class="controls"><span class="icon">⌯⌲</span> <span>${toK(Number(p.replies||0))}</span></div></div>`; return wrapper; }
function showCharacters(){ const left=document.getElementById('leftCol'); const right=document.getElementById('rightCol'); left.innerHTML=''; right.innerHTML=''; const card=document.createElement('div'); card.className='card'; card.innerHTML=`<div style="display:flex;justify-content:space-between;align-items:center"><strong>Characters</strong><button id="newCharBtn" class="smalllink">+ New Character</button></div><div id="charsCompact" style="margin-top:8px"></div>`; left.appendChild(card); card.querySelector('#newCharBtn').onclick=()=>openCharacterEditor(); const compact=card.querySelector('#charsCompact'); compact.innerHTML=''; for(const c of STATE.characters){ const row=document.createElement('div'); row.className='charRow'; row.innerHTML = `<img class="charAvatar" src="${esc(c.img||'data:image/svg+xml;utf8,<svg xmlns=\'http://www.w3.org/2000/svg\' width=\'120\' height=\'120\'><rect width=\'100%\' height=\'100%\' fill=\'%23ffffff\' /></svg>')}" onerror="this.style.display='none'"><div style="flex:1"><div style="font-weight:700">${esc(c.displayName)}</div><div class="meta">@${esc(c.username)}</div></div><div><button class="smalllink" onclick="viewProfile('${c.id}')">View</button></div>`; compact.appendChild(row); } renderCharacterListSmall(); }
window.viewProfile=function(charId){ const left=document.getElementById('leftCol'); const right=document.getElementById('rightCol'); const c=STATE.characters.find(x=>x.id===charId); if(!c) return alert('Character not found'); left.innerHTML=''; right.innerHTML=''; const wrapper=document.createElement('div'); wrapper.className='profile-card'; wrapper.innerHTML = `<button class="back-btn" id="backFromProfile">← Back</button><div class="profileHeader" style="background-image:url('${esc(c.header||'data:image/svg+xml;utf8,<svg xmlns=\'http://www.w3.org/2000/svg\' width=\'800\' height=\'200\'><rect width=\'100%\' height=\'100%\' fill=\'%23ffffff\' /></svg>')}')"></div><img class="profileAvatar" src="${esc(c.img||'data:image/svg+xml;utf8,<svg xmlns=\'http://www.w3.org/2000/svg\' width=\'200\' height=\'200\'><rect width=\'100%\' height=\'100%\' fill=\'%23ffffff\' /></svg>')}" onerror="this.style.display='none'"><div class="card profile-card"><div class="profile-name" style="text-align:center"><div class="profile-display">${esc(c.displayName)}</div><div class="profile-username">@${esc(c.username)}</div></div><div class="profile-divider"></div><div style="display:flex;justify-content:center;gap:12px"><button class="smalllink" id="sendDMBtn">Send a DM</button><button class="smalllink" id="viewPostsBtn">View Posts</button></div><div class="profile-divider"></div><div class="profile-bio">${esc(c.bio||'')}</div><div class="follow-counts"><div>Following: <strong>${esc(c.following||0)}</strong></div><div>Followers: <strong>${esc(c.followers||0)}</strong></div></div><div class="profile-divider"></div><div class="section-title">𖣠 | DESCRIPTION</div><div class="section-box" id="descBox">${c.description? c.description : ''}</div><div class="profile-divider"></div><div class="section-title">𖣠 | RELATIONSHIPS</div><div class="section-box" id="relBox">${c.relationships? c.relationships : ''}</div><div style="margin-top:12px"><button class="btn smalllink" id="editProfileBtn">Edit Profile</button></div></div>`; left.appendChild(wrapper); document.getElementById('backFromProfile').onclick=()=>showCharacters(); document.getElementById('sendDMBtn').onclick=()=>openChatById(c.id); document.getElementById('viewPostsBtn').onclick=()=>showFeed(c.id); document.getElementById('editProfileBtn').onclick=()=>openCharacterEditor(c.id); renderCharacterListSmall(); }
function renderCharacterListSmall(){ const right=document.getElementById('rightCol'); if(!right) return; right.innerHTML=''; const card=document.createElement('div'); card.className='card'; card.innerHTML=`<strong>Characters</strong><div style="margin-top:8px" id="smallList"></div>`; right.appendChild(card); const smallList=card.querySelector('#smallList'); for(const c of STATE.characters){ const row=document.createElement('div'); row.className='charRow'; row.innerHTML = `<img class="charAvatar" src="${esc(c.img||'data:image/svg+xml;utf8,<svg xmlns=\'http://www.w3.org/2000/svg\' width=\'120\' height=\'120\'><rect width=\'100%\' height=\'100%\' fill=\'%23ffffff\' /></svg>')}" onerror="this.style.display='none'"><div style="flex:1"><div style="font-weight:700">${esc(c.displayName)}</div><div class="meta">@${esc(c.username)}</div></div><div><button class="smalllink" onclick="viewProfile('${c.id}')">View</button></div>`; smallList.appendChild(row); } }
window.openCharacterEditor=function(charId){ const left=document.getElementById('leftCol'); left.innerHTML=''; let obj={id:uid('char'),displayName:'',username:'',bio:'',description:'',relationships:'',img:'',header:'',followers:0,following:0}; if(charId){ const found=STATE.characters.find(x=>x.id===charId); if(found) obj=found; } const form=document.createElement('div'); form.className='card'; form.innerHTML = `<h3>${charId? 'Edit Character':'New Character'}</h3><label class="label">Header image</label><input type="file" id="headerFile"><label class="label">Profile image</label><input type="file" id="imgFile"><label class="label">Display name</label><input id="displayName" class="input" value="${esc(obj.displayName)}"><label class="label">Username</label><input id="username" class="input" value="${esc(obj.username)}"><label class="label">Bio</label><input id="bio" class="input" value="${esc(obj.bio)}"><label class="label">Followers</label><input id="followers" class="input" type="number" value="${esc(obj.followers||0)}"><label class="label">Following</label><input id="following" class="input" type="number" value="${esc(obj.following||0)}"><label class="label">Character description (rich text)</label><div id="descEdit" contenteditable="true" class="section-box">${obj.description||''}</div><label class="label">Relationship notes (rich text)</label><div id="relEdit" contenteditable="true" class="section-box">${obj.relationships||''}</div><div style="margin-top:12px"><button id="saveChar" class="btn smalllink">Save</button> <button id="cancelChar" class="smalllink">Cancel</button></div>`; left.appendChild(form); const headerFile=document.getElementById('headerFile'); const imgFile=document.getElementById('imgFile'); headerFile.onchange=function(e){ const f=e.target.files[0]; if(!f) return; const r=new FileReader(); r.onload=function(){ headerFile._data=r.result; }; r.readAsDataURL(f); }; imgFile.onchange=function(e){ const f=e.target.files[0]; if(!f) return; const r=new FileReader(); r.onload=function(){ imgFile._data=r.result; }; r.readAsDataURL(f); }; document.getElementById('cancelChar').onclick=()=>showCharacters(); document.getElementById('saveChar').onclick=()=>{ const data=loadState(); const c={ id:obj.id, displayName:document.getElementById('displayName').value||'Unnamed', username:document.getElementById('username').value||('user'+Math.floor(Math.random()*9999)), bio:document.getElementById('bio').value||'', followers:Number(document.getElementById('followers').value||0), following:Number(document.getElementById('following').value||0), description:sanitizeHtml(document.getElementById('descEdit').innerHTML||''), relationships:sanitizeHtml(document.getElementById('relEdit').innerHTML||''), img:imgFile._data||obj.img||'', header:headerFile._data||obj.header||'' }; const idx=data.characters.findIndex(x=>x.id===c.id); if(idx>=0) data.characters[idx]=c; else data.characters.push(c); STATE=data; saveState(); showCharacters(); }; }
function showDMs(){ const left=document.getElementById('leftCol'); left.innerHTML=''; const card=document.createElement('div'); card.className='card'; card.innerHTML=`<h3>DMs</h3><div id="dmList"></div>`; left.appendChild(card); const list=document.getElementById('dmList'); list.innerHTML=''; for(const c of STATE.characters){ const b=document.createElement('button'); b.textContent=c.displayName||c.username; b.onclick=()=>openChatById(c.id); list.appendChild(b); } }
function openChatById(charId){ const left=document.getElementById('leftCol'); left.innerHTML=''; const c=STATE.characters.find(x=>x.id===charId); if(!c) return alert('Not found'); const card=document.createElement('div'); card.className='card'; const convId='dm-'+charId; STATE.dms[convId]=STATE.dms[convId]||[]; card.innerHTML=`<h3>DM — ${esc(c.displayName)}</h3><div id="msgs" style="max-height:300px;overflow:auto;margin-bottom:8px"></div><input id="msgIn" class="input" placeholder="Type a message..."> <button id="sendMsg" class="smalllink">Send</button>`; left.appendChild(card); const msgs=document.getElementById('msgs'); function refresh(){ msgs.innerHTML = STATE.dms[convId].map(m=>`<div class="dmMsg ${m.from==='me'?'dmYou':'dmThem'}">${esc(m.from==='me'?'You':c.displayName)}: ${esc(m.text)}</div>`).join(''); msgs.scrollTop=msgs.scrollHeight; } refresh(); document.getElementById('sendMsg').onclick=()=>{ const t=document.getElementById('msgIn').value.trim(); if(!t) return; STATE.dms[convId].push({from:'me',text:t,ts:Date.now()}); const reply=`${c.displayName}: ${c.description? c.description.replace(/<[^>]+>/g,'').split('.')[0] : '...'} replies to "${t}"`; STATE.dms[convId].push({from:'them',text:reply,ts:Date.now()}); saveState(); refresh(); document.getElementById('msgIn').value=''; }; }
function showLore(){ const left=document.getElementById('leftCol'); left.innerHTML=''; const card=document.createElement('div'); card.className='card'; card.innerHTML=`<h3>Universe Lore</h3><textarea id="loreText" class="input" style="height:240px">${esc(STATE.lore||'')}</textarea><div style="margin-top:8px"><button id="saveLore" class="smalllink">Save</button></div>`; left.appendChild(card); document.getElementById('saveLore').onclick=()=>{ STATE.lore=document.getElementById('loreText').value; saveState(); alert('Saved'); }; }
function showSettings(){ const left=document.getElementById('leftCol'); left.innerHTML=''; const card=document.createElement('div'); card.className='card'; card.innerHTML=`<h3>Settings</h3><div class="card">Settings will be added later.</div>`; left.appendChild(card); }
function renderCharacterListSmall(){ const right=document.getElementById('rightCol'); if(!right) return; right.innerHTML=''; const card=document.createElement('div'); card.className='card'; card.innerHTML=`<strong>Characters</strong><div style="margin-top:8px" id="smallList"></div>`; right.appendChild(card); const smallList=card.querySelector('#smallList'); for(const c of STATE.characters){ const row=document.createElement('div'); row.className='charRow'; row.innerHTML = `<img class="charAvatar" src="${esc(c.img||'data:image/svg+xml;utf8,<svg xmlns=\'http://www.w3.org/2000/svg\' width=\'120\' height=\'120\'><rect width=\'100%\' height=\'100%\' fill=\'%23ffffff\' /></svg>')}" onerror="this.style.display='none'"><div style="flex:1"><div style="font-weight:700">${esc(c.displayName)}</div><div class="meta">@${esc(c.username)}</div></div><div><button class="smalllink" onclick="viewProfile('${c.id}')">View</button></div>`; smallList.appendChild(row); } }
renderApp();
